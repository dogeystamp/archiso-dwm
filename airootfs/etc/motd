Arch ISO dwm

Start dwm via startx. If your device has poor performance, you can use

	startx ~/minxinitrc

instead.

The escape key and the caps lock key are swapped in the GUI. This can be disabled
by removing the setxkbmap line in .xinitrc or minxinitrc.

Qutebrowser and Firefox are available.
